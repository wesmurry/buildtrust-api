DATABASE_URL=postgresql://buildtrust:buildtrust@localhost:5432/buildtrust
REDIS_URL=redis://localhost:6379

# Auth (optional for dev — uses header bypass when not set)
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=

# AI (optional — stubs used when not set)
ANTHROPIC_API_KEY=

# Storage (optional — local filesystem used when not set)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=
AWS_REGION=

# Email (optional — logs to console when not set)
RESEND_API_KEY=
FROM_EMAIL=bids@buildtrust.app

# App
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:8080
